lint:
 stage: test
 script:
 - pip install yapf
 - yapf --style google -i --recursive .
 only:
 - pushes

unittest:
 stage: test
 script:
  - pip install -r requirements.txt
  - pytest .
 only:
  - merge_requests